<view class="section">
  <input class="input" placeholder="输入关键字"  type="text" bindinput="bindKeyInput"/>
</view>
<view class="bg-white">
  <view bindtap="setAdd" wx:for="{{sugData}}" wx:for-item="list" wx:key="uid" data-name="{{list.name}}" class="list">
    {{list.name}}
  </view>  
  <!-- -->
</view>